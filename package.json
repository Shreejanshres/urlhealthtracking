{
  "name": "urltracking",
  "version": "1.0.0",
  "private": true,
  "description": "Bulk Url Health Checker",
  "main": "index.js",
  "scripts": {
    "dev": "docker compose up -d && wait-on tcp:5432 tcp:6379 && pnpm --parallel --filter './apps/*' dev",
    "build": "pnpm --recursive build",
    "test": "pnpm --recursive test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.16.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "wait-on": "^9.1.0"
  }
}